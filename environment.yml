# A conda environment with all useful package for ctamclib developers
name: tgb
dependencies:
  - pyyaml
  - pathlib
  - numpy
  - astropy
  - matplotlib
  - sphinx
  - pytest
  - pytest-cov
  - naima
  - iminuit

# cheatsheet

# create: conda env create -f environment.yml
# activate: conda activate gammasim-tools-dev
# update: conda env update -f environment.yml --prune
